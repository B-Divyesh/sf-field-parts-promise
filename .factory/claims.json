[
  {
    "id": "promise-status-from-allocation",
    "claim": "The promise status changes from Date at risk to Parts in hand only after every required quantity has an allocation.",
    "where": "M1 demo job status plate and README",
    "test": "npm run test:e2e -- --grep @claim:promise-status-from-allocation",
    "sandbox": "fresh browser context at /?demo=1; use bundled RD-1042 data; allocate the missing pump and assert the status reason before and after"
  },
  {
    "id": "allocation-keeps-source",
    "claim": "Each allocation keeps its job, source, quantity, unit, updater, and checked time after reload.",
    "where": "M1 required-part row and allocation review",
    "test": "npm run test:e2e -- --grep @claim:allocation-keeps-source",
    "sandbox": "fresh browser context at /?demo=1; allocate from Van 2, reload, and assert all six displayed fields from IndexedDB"
  },
  {
    "id": "reorder-after-allocation",
    "claim": "Using the last spare in Van 2 suggests a reorder but never places one.",
    "where": "M1 allocation result and landing live preview",
    "test": "npm run test:e2e -- --grep @claim:reorder-after-allocation",
    "sandbox": "fresh browser context at /?demo=1; allocate the pump, assert the below-minimum calculation and suggestion, and assert no supplier order or network write exists"
  },
  {
    "id": "demo-reset-isolated",
    "claim": "Demo changes reset to the bundled sample and never appear in the live local workspace.",
    "where": "M1 demo banner, privacy page, demo guide, and README",
    "test": "npm run test:e2e -- --grep @claim:demo-reset-isolated",
    "sandbox": "fresh browser context; change RD-1042 in demo, reset it, then leave demo and assert the separate live database is empty"
  },
  {
    "id": "offline-reload",
    "claim": "The sample job and allocation flow work offline after the first visit.",
    "where": "M1 landing fact, offline plate, and README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /?demo=1; wait for service-worker readiness, set the context offline, reload, allocate the sample pump, and assert the updated status"
  },
  {
    "id": "local-workspace-flow",
    "claim": "One person can create a local job and required part, then record a van or warehouse source. They can allocate and undo a quantity, attach supplier-date evidence, and review the promise status.",
    "where": "landing how-it-works section and README M1 description",
    "test": "npm run test:e2e -- --grep @claim:local-workspace-flow",
    "sandbox": "fresh browser context; create a live local job and source, allocate then undo it, then attach supplier evidence in the isolated sample"
  },
  {
    "id": "m1-feature-boundaries",
    "claim": "This release has no sign-in, team sync, barcode scan, supplier-order action, or checkout.",
    "where": "landing facts and scope section, terms, and README",
    "test": "npm run test:e2e -- --grep @claim:m1-feature-boundaries",
    "sandbox": "fresh browser context; assert the shipped scope copy and the absence of sign-in, invite, scan, order, buy, and checkout actions"
  },
  {
    "id": "indexeddb-local-storage",
    "claim": "Jobs, required parts, sources, and allocations are stored in the named browser IndexedDB workspaces.",
    "where": "privacy page and README architecture section",
    "test": "npm run test:e2e -- --grep @claim:indexeddb-local-storage",
    "sandbox": "fresh browser context; allocate the sample pump, read the stored workspace from parts-promise-demo-v1, then open live mode and assert parts-promise-live-v1 exists"
  },
  {
    "id": "demo-network-privacy",
    "claim": "The demo makes only same-origin GET requests and never asks for camera access.",
    "where": "privacy page, demo guide, and README architecture section",
    "test": "npm run test:e2e -- --grep @claim:demo-network-privacy",
    "sandbox": "fresh browser context with a camera-call sentinel and full request log; allocate and reset the sample, then assert every request is a same-origin GET or HEAD and camera was never requested"
  },
  {
    "id": "clear-local-records",
    "claim": "Browser site-data controls remove local workspace records.",
    "where": "privacy page",
    "test": "npm run test:e2e -- --grep @claim:clear-local-records",
    "sandbox": "fresh Chromium context; create a live job, clear IndexedDB and local storage through the browser storage controls, reload, and assert the live workspace is empty"
  },
  {
    "id": "container-runtime",
    "claim": "The production server starts with only PORT, serves its build identity and compiled app, has no job-data endpoint, and returns HTTP 404 for unknown paths.",
    "where": "README architecture and deployment sections",
    "test": "npm run test:e2e -- --grep @claim:container-runtime",
    "sandbox": "build and spawn the real Rust server with an environment containing only PORT; probe /health, /, a job-data request, and an unknown path; inspect the shipped image user and base contract"
  }
]
