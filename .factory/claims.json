[
  {
    "id": "sample-fixture",
    "claim": "The demo opens Riverside Dental job RD-1042 with one missing condensate pump.",
    "where": "landing action outcome, landing sample preview, README sample guide, and demo guide",
    "test": "npm run test:e2e -- --grep @claim:sample-fixture",
    "sandbox": "fresh browser context at /?demo=1; assert Riverside Dental, RD-1042, Condensate pump, zero held, and the one-unit shortage"
  },
  {
    "id": "promise-status-from-allocation",
    "claim": "The promise status changes from Date at risk to Parts in hand only after every required quantity has an allocation.",
    "where": "M1 demo job status plate and README",
    "test": "npm run test:e2e -- --grep @claim:promise-status-from-allocation",
    "sandbox": "fresh browser context at /?demo=1; use bundled RD-1042 data; allocate the missing pump and assert the status reason before and after"
  },
  {
    "id": "allocation-keeps-source",
    "claim": "Each allocation keeps its job, source, quantity, unit, updater, and checked time after reload.",
    "where": "M1 required-part row and allocation review",
    "test": "npm run test:e2e -- --grep @claim:allocation-keeps-source",
    "sandbox": "fresh browser context at /?demo=1; allocate from Van 2, reload, and assert all six displayed fields from IndexedDB"
  },
  {
    "id": "supplier-quantity-conserved",
    "claim": "One supplier-order unit can be held for only one job.",
    "where": "supplier allocation picker and promise status",
    "test": "npm run test:e2e -- --grep @claim:supplier-quantity-conserved",
    "sandbox": "fresh browser context at /?demo=1; attach a one-unit supplier order to RD-1042, add a second job requiring the same part, and assert the consumed order is unavailable and only the first job is covered"
  },
  {
    "id": "reorder-after-allocation",
    "claim": "Using the last spare in Van 2 suggests a reorder but never places one.",
    "where": "M1 allocation result and landing live preview",
    "test": "npm run test:e2e -- --grep @claim:reorder-after-allocation",
    "sandbox": "fresh browser context at /?demo=1; allocate the pump, assert the below-minimum calculation and suggestion, and assert no supplier order or network write exists"
  },
  {
    "id": "demo-reset-isolated",
    "claim": "Demo changes reset to the bundled sample, and every demo exit discards them before reopening the unchanged live local workspace.",
    "where": "M1 demo banner, privacy page, demo guide, and README",
    "test": "npm run test:e2e -- --grep @claim:demo-reset-isolated",
    "sandbox": "fresh browser context; create a live job, change RD-1042, leave by wordmark and browser Back, assert the demo database is deleted each time, then use the confirmed exit and assert the live job survives unchanged"
  },
  {
    "id": "offline-reload",
    "claim": "The sample job and allocation flow work offline after the first visit.",
    "where": "M1 landing fact, offline plate, and README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /?demo=1; wait for service-worker readiness, set the context offline, reload, allocate the sample pump, and assert the updated status"
  },
  {
    "id": "local-workspace-flow",
    "claim": "One person can create a local job and required part, then record a van or warehouse source. They can allocate and undo a quantity, attach supplier-date evidence, and review the promise status.",
    "where": "landing how-it-works section and README M1 description",
    "test": "npm run test:e2e -- --grep @claim:local-workspace-flow",
    "sandbox": "fresh browser context; create a live local job and source, allocate then undo it, attach supplier evidence to that same user-created required part, and review its promise status"
  },
  {
    "id": "demo-feature-boundaries",
    "claim": "The demo stays account-free and cannot sync, scan, place an order, or start payment.",
    "where": "demo banner, landing scope section, and demo guide",
    "test": "npm run test:e2e -- --grep @claim:demo-feature-boundaries",
    "sandbox": "fresh browser context at /?demo=1; assert no sign-in, invite, scan, order, or checkout action exists"
  },
  {
    "id": "indexeddb-local-storage",
    "claim": "Jobs, required parts, sources, and allocations are stored in the named browser IndexedDB workspaces.",
    "where": "privacy page and README architecture section",
    "test": "npm run test:e2e -- --grep @claim:indexeddb-local-storage",
    "sandbox": "fresh browser context; allocate the sample pump, read the stored workspace from parts-promise-demo-v1, then open live mode and assert parts-promise-live-v1 exists"
  },
  {
    "id": "demo-network-privacy",
    "claim": "The demo makes only same-origin GET requests and never asks for camera access.",
    "where": "privacy page, demo guide, and README architecture section",
    "test": "npm run test:e2e -- --grep @claim:demo-network-privacy",
    "sandbox": "fresh browser context with a camera-call sentinel and full request log; allocate and reset the sample, then assert every request is a same-origin GET or HEAD and camera was never requested"
  },
  {
    "id": "clear-local-records",
    "claim": "Browser site-data controls remove local workspace records.",
    "where": "privacy page",
    "test": "npm run test:e2e -- --grep @claim:clear-local-records",
    "sandbox": "fresh Chromium context; create a live job, clear IndexedDB and local storage through the browser storage controls, reload, and assert the live workspace is empty"
  },
  {
    "id": "workspace-backup-roundtrip",
    "claim": "Export workspace downloads a versioned JSON backup that restores every job, required part, source, allocation, and timestamp.",
    "where": "jobs toolbar, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:workspace-backup-roundtrip",
    "sandbox": "fresh demo workspace; export JSON, deep-compare every original record and timestamp to the download, change the sample, import the backup after preview, and deep-compare the restored workspace"
  },
  {
    "id": "csv-import-validation",
    "claim": "Import workspace previews CSV jobs, required parts, and sources with row-level validation before saving.",
    "where": "jobs import sheet and README",
    "test": "npm run test:e2e -- --grep @claim:csv-import-validation",
    "sandbox": "fresh live workspace; preview and import valid CSV rows, then preview an invalid quantity and assert its row number is shown and saving is unavailable"
  },
  {
    "id": "demo-transfer-isolated",
    "claim": "Importing or exporting the sample uses only the demo workspace and never changes the live workspace.",
    "where": "jobs import sheet, demo guide, README",
    "test": "npm run test:e2e -- --grep @claim:demo-transfer-isolated",
    "sandbox": "fresh browser context; create and snapshot a distinct live workspace, export the demo and inspect it, import a different CSV job while in demo, leave demo, then byte-compare the live workspace to its snapshot"
  },
  {
    "id": "csv-template-download",
    "claim": "Download CSV template provides a CSV file with a valid job, required part, and source example.",
    "where": "jobs import sheet and README",
    "test": "npm run test:e2e -- --grep @claim:csv-template-download",
    "sandbox": "fresh live workspace; download the template, assert its filename, text/csv media type, exact header, and example rows, then preview it without an error"
  },
  {
    "id": "entra-sign-in",
    "claim": "Account sign-in uses the shared Sociobot Microsoft Entra tenant, and invalid or expired tokens are rejected.",
    "where": "header, onboarding, and API authentication",
    "test": "npm run test:e2e -- --grep @claim:entra-sign-in",
    "sandbox": "fresh browser context; assert the authorization request tenant, client, and callback, then send an expired test-issuer token and assert 401 plus WWW-Authenticate"
  },
  {
    "id": "tenant-data-isolation",
    "claim": "A signed-in firm cannot read another firm's shared workspace.",
    "where": "privacy page and server data boundary",
    "test": "npm run test:e2e -- --grep @claim:tenant-data-isolation",
    "sandbox": "clean test database; onboard two test-token identities into different firms, store a distinct job in firm A, and assert firm B bootstrap never returns it"
  },
  {
    "id": "two-device-sync",
    "claim": "A saved firm workspace appears on a second signed-in device.",
    "where": "jobs sync status and README",
    "test": "npm run test:e2e -- --grep @claim:two-device-sync",
    "sandbox": "two fresh browser contexts using one test identity; push a workspace from the first and pull it from the second"
  },
  {
    "id": "idempotent-sync",
    "claim": "Retrying the same sync operation applies it only once.",
    "where": "server sync behavior and README",
    "test": "npm run test:e2e -- --grep @claim:idempotent-sync",
    "sandbox": "clean test database; send the same UUID operation twice and assert one version increase plus a replayed response"
  },
  {
    "id": "offline-signed-in-sync",
    "claim": "A signed-in change made offline stays in a durable device outbox, survives reload, retries on reconnect, and backs off after a temporary sync failure.",
    "where": "offline plate, jobs sync status, README",
    "test": "npm run test:e2e -- --grep @claim:offline-signed-in-sync",
    "sandbox": "authenticated test firm in a fresh browser context; go offline, add a job, assert one IndexedDB outbox operation, reload offline, reconnect, assert the server receives the job, inject one 503, and assert automatic retry without another user action"
  },
  {
    "id": "sync-conflict-resolution",
    "claim": "A stale signed-in quantity revision opens an explicit conflict and cannot overwrite the shared quantity evidence.",
    "where": "shared workspace conflict sheet and README",
    "test": "npm run test:e2e -- --grep @claim:sync-conflict-resolution",
    "sandbox": "authenticated test firm; queue an offline required quantity, advance the server revision, reconnect, assert the two-revision conflict, no device-overwrite action, and unchanged server data"
  },
  {
    "id": "invitation-email-activation",
    "claim": "A recorded team invitation activates only when its matching verified Entra email signs in.",
    "where": "team settings and README",
    "test": "npm run test:e2e -- --grep @claim:invitation-email-activation",
    "sandbox": "clean test firm; invite one email, assert a different signed token remains unassigned, then assert the matching signed token receives the invited technician role"
  },
  {
    "id": "account-service-boundaries",
    "claim": "Signed-in workspace data uses this product API, and sign-in redirects to the shared Sociobot Microsoft Entra tenant.",
    "where": "privacy page and README",
    "test": "npm run test:e2e -- --grep @claim:account-service-boundaries",
    "sandbox": "fresh authenticated context with request logging; open account data controls and assert same-origin requests, then use a signed-out context and assert the sign-in request targets the configured Entra tenant"
  },
  {
    "id": "audit-log-recording",
    "claim": "Firm onboarding, invitation, and workspace sync actions appear in the firm's exported audit events.",
    "where": "privacy page, data controls, and README",
    "test": "npm run test:e2e -- --grep @claim:audit-log-recording",
    "sandbox": "clean test firm; onboard, invite, sync, export, and assert the three named audit event types"
  },
  {
    "id": "firm-deletion-hold",
    "claim": "A firm owner can schedule deletion with a 14-day hold and cancel it before deletion.",
    "where": "data controls, privacy page, and README",
    "test": "npm run test:e2e -- --grep @claim:firm-deletion-hold",
    "sandbox": "clean owner test firm; schedule deletion with the exact firm name, assert the 14-day interval, cancel it, and assert bootstrap reports no scheduled deletion"
  },
  {
    "id": "response-policy",
    "claim": "Export uses a five-request critical bucket with a positive Retry-After, and protected metrics expose latency, status, conflict, queue-age, and notification signals.",
    "where": "README operations section and milestone handoff",
    "test": "npm run test:e2e -- --grep @claim:response-policy",
    "sandbox": "clean test firm; exceed the export burst, assert 429 and Retry-After >= 1, authenticate to metrics, and assert every documented operations series"
  },
  {
    "id": "subscription-checkout",
    "claim": "If the recurring Sociobot test product is unavailable, checkout stops before any charge and explains the operator action.",
    "where": "billing settings",
    "test": "npm run test:e2e -- --grep @claim:subscription-checkout",
    "sandbox": "authenticated test firm; call the real pilot gateway adapter and assert its unregistered-product response becomes HTTP 424 with no Dodo request"
  },
  {
    "id": "technician-seat-charge",
    "claim": "Workshop costs $39 per month, each active technician costs $8 per month, and the owner is excluded from the technician seat total.",
    "where": "team and billing settings",
    "test": "npm run test:e2e -- --grep @claim:technician-seat-charge",
    "sandbox": "fresh landing page plus a clean test firm with one owner and one technician; assert the two stated prices and exactly one technician seat"
  },
  {
    "id": "expired-plan-keeps-export",
    "claim": "An unpaid firm keeps existing records and can export them while new cloud writes are blocked.",
    "where": "billing settings, API error, and terms",
    "test": "npm run test:e2e -- --grep @claim:expired-plan-keeps-export",
    "sandbox": "clean test firm with one stored job and an unpaid recorded entitlement; assert sync returns 402 and export returns the job"
  },
  {
    "id": "container-runtime",
    "claim": "The production server starts with only PORT, serves its build identity and compiled app, rate-limits API traffic, and returns HTTP 404 for unknown paths.",
    "where": "README architecture and deployment sections",
    "test": "npm run test:e2e -- --grep @claim:container-runtime",
    "sandbox": "build and spawn the real Rust server with an environment containing only PORT; probe /health, /, an API burst, and an unknown path; assert 429 plus Retry-After and inspect the image contract"
  }
]
